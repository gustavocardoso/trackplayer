import React from 'react'
import GlobalStyle from './styles/global'
import { Container, Title } from './styles/base'

const Admin = () => (
  <div>
    <GlobalStyle />
    <Container>
      <Title>Backend</Title>
      <p>This is just a simple paragraph text.</p>
    </Container>
  </div>
)

export default Admin
